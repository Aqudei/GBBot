<UserControl x:Class="GMB_And_Selenium.Views.ProjectListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GMB_And_Selenium.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <Button x:Name="ImportProjects" Content="Import Projects" Margin="2"/>
            <Button x:Name="StartBot" Content="START" Margin="2"/>
            <Button x:Name="ImportProjects_Copy1" Content="STOP" Margin="2"/>
        </StackPanel>
        <DataGrid x:Name="Projects" Grid.Row="1" AutoGeneratingColumn="Projects_AutoGeneratingColumn" Margin="2" IsReadOnly="True"/>
    </Grid>
</UserControl>
